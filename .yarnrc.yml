approvedGitRepositories:
  - "**"

enableScripts: false

nodeLinker: node-modules

npmMinimalAgeGate: 2800

npmScopes:
  navikt:
    npmAlwaysAuth: true
    npmAuthToken: "${NODE_AUTH_TOKEN:-}"
    npmRegistryServer: "https://npm.pkg.github.com"
